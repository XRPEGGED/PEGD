# Names only — set real values via Cloudflare Pages secrets / local env.
# Do not put real secrets in this file or commit them.

# Portal (Cloudflare Pages secrets)
PORTAL_SESSION_SECRET=
PORTAL_ALLOWLIST=
PORTAL_ORIGINS=
HOLDER_MIN_PEGD=

# Xaman / XUMM
XUMM_API_KEY=
XUMM_API_SECRET=

# Supabase (environment-specific; set SUPABASE_URL via secret, not in-repo)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Bridge / relayer (if deploying workers)
GUARDIAN_SECRET_KEY=
RELAYER_KEYPAIR=
RELAYER_SOLANA_KEY=
RELAYER_XRPL_SEED=
BRIDGE_PEGD_ACCOUNT=
BRIDGE_PDA=
